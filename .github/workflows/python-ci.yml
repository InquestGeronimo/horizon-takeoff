name: Python CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9  # Use Python 3.9 instead of 3.10

    - name: Install dependencies
      run: |
        pip install -U pip
        pip install ruff  # Install the 'ruff' library

    - name: Run tests
      run: |
        # Replace 'your_test_command' with the actual test command for your project
        ruff check horizon/
        ruff format horizon/
        ruff check client.py
        ruff format client.py

    - name: Upload test results
      uses: actions/upload-artifact@v2
      with:
        name: test-results
        path: .
